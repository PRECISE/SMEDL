image: python:3.5

before_script:
  - python -V
  - which python

test:
  script:
  - pyvenv .env && source .env/bin/activate
  - ls -la .env/bin
  - python -V
  - which python
  - pip install .
  - nosetests
