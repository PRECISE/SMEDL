image: python:3.5

before_script:
  - apt-get update -qq && apt-get install -y -qq python3-pip
  - python -V

test:
  script:
  - pyvenv .env && source .env/bin/activate
  - pip install .
  - nosetests
