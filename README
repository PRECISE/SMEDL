SMEDL/PEDL Monitor Generator
Version 1.0

Requirements
------------
1. Python 3
2. Pip
    - Grako 3.6
    - Jinja2 2.8

Getting started
---------------
    A Python virtual environment (PUT LINK HERE) including Python 3.4.3 and all
required Python packages has been created to simplify the process of getting
started with the tool. To start the virtual environment, run the following
command from the project's root directory:

    source gramma_env/bin/activate

    NOTE: If you choose to not use the virtual environment, you can simply
install the required Python packages using the following command:

    pip install -r requirements.txt


Running the tool
----------------
    The 'mgen' script is the primary interface for generating software monitors
from SMEDL and PEDL definitions. This script can be run with the following
command:

    python src/mgen.py PEDL_FILE SMEDL_FILE


Manually run intermediate generation steps
------------------------------------------

-- To generate a SMEDL parser using the grammar --
      grako smedl.grako -o smedl_parser.py
    ( grako   GRAMMAR   -o  OUTPUT_PARSER  )

-- To parse a SMEDL file using the generated parser --
     python smedl_parser.py example.smedl  object
   ( python     PARSER       INPUT_FILE  START_RULE )

(Use '-t' command-line option to enable debug tracing)


Updating from the repository
----------------------------
POST THIS PROJECT ON GITHUB?


Changelog
---------
1.0: (August 6, 2015)
    - PUT FEATURES HERE