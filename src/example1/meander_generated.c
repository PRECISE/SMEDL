enum { SWITCH, SAFEMON } stateset;

struct {
  int upbound;
  int lobound;
};
stateset currentState = SWITCH;

void changeDir() {
  switch (currentState) {
  }
}

void updatePos(int pos) {
  switch (currentState) {
  }
}

