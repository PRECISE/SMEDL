object WebServerMon
    identity
        opaque id;
    state
        URL url;
        HTTPStatusCode code;
    events
        imported request(URL), response(HTTPStatusCode);
    scenarios
        Authentication:
        WebServer -> request(url) when authRequired(url) -> response(code) when code == 401 -> WebServer
            else { response(403) } -> WebServer