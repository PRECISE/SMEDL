//for each input event with string, output same event 
//test assignment of string type
object stringtest
 state
   string name;
   int value;
 events
   imported ping(string, int);
   internal trans();
   exported pong(string,int);
 scenarios
   sc1:
     Start -> ping(st, x){name = st; value = x; raise trans();} -> End
   sc2:
     Start -> trans(){raise pong(name, value);} -> End
